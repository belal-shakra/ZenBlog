<div class="form-group mb-2">
    <textarea
    name="{{ $name }}"
    id="{{ $name }}"
    cols="30"
    rows="10"
    class="form-control border border-1 border-secondary"
    placeholder="{{ $placeholder }}"
    >{{ $value }}</textarea>
</div>
